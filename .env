# MyFirstProject environment file (cleaned)
# This project uses Firebase via native configuration files
# (android/app/google-services.json and iOS GoogleService-Info.plist).
# Keep secrets out of git; only store non-sensitive flags here.

NODE_ENV=development
REACT_NATIVE_APP_ENV=development
ENABLE_ANALYTICS=false

# If you need to provide JS-level Firebase config instead of native files,
# add the keys below and fill them. By default this app uses native configs.
# FIREBASE_API_KEY=
# FIREBASE_AUTH_DOMAIN=
# FIREBASE_PROJECT_ID=
# FIREBASE_STORAGE_BUCKET=
# FIREBASE_MESSAGING_SENDER_ID=
# FIREBASE_APP_ID=

# Optional external API (not referenced by current codebase)
# API_URL=

# Error reporting (optional)
# SENTRY_DSN=